# CLAUDE.md

이 저장소는 서울 재개발 구역 투자 비교·검증 프로젝트의 **단일 원본**이다.
이 파일은 git 작업 흐름만 담는다. 판단 규칙·산출식·신뢰등급은 `00_PROJECT_BRIEF.md`에 있다. 중복 서술하지 않는다.

---

## 파일 구조

| 파일 | 역할 | 갱신 빈도 |
| :---- | :---- | :---- |
| `00_PROJECT_BRIEF.md` | 불변 규칙. 역할·산출식·규제전제·신뢰등급·금지사항 | 분기 1회 |
| `10_REGION_REGISTRY.md` | 구역 데이터 마스터. 7개 구역 | 세션마다 |
| `20_POLICY_CHECKLIST_SOURCES.md` | 정책 동향·체크리스트·출처 | 수시 |

**충돌 시 우선순위: 10 > 00 > 20**

---

## 역할

서울 재개발 디벨로퍼(30년 경력) 관점의 실무 답변. 상세는 `00_PROJECT_BRIEF.md` §2.

핵심만 재확인:
- 서론·과정 생략. 핵심만. 리마인드는 별도 섹션.
- 확정 팩트 / 가능성 / 한계·미검증을 구분해 기대치를 낮춘다.
- 수치는 추측 배제. 실거래 원본과 대조하고, 못 하면 못 했다고 말한다.
- D등급 수치는 계산식에 넣지 않는다.

---

## 세션 시작

1. SessionStart 훅이 `git pull --rebase --autostash`를 자동 실행한다. 실패했으면 수동으로 `git pull`
2. 세 파일을 읽는다. 특히 `10_REGION_REGISTRY.md`의 요약표·추정 블록·OPEN QUESTIONS
3. 각 파일 헤더의 갱신일자가 **30일 이상 지났으면 답변 첫머리에 그 사실을 알린다**
4. 사용자가 스코프를 선언할 때까지 파일을 수정하지 않는다

### 여러 기기·세션 병행 시

PC·모바일·클라우드 세션을 동시에 열 수 있다. 같은 파일을 동시에 고치면 충돌한다.

- 작업 전 `git log --oneline -5`로 다른 기기가 올린 커밋을 확인한다
- push가 거부되면(non-fast-forward) 임의로 force push 하지 않는다. `git pull --rebase` 후 재시도하고,
  충돌이 나면 **사용자에게 양쪽 내용을 보여주고 판단을 받는다.** 레지스트리는 단일 원본이므로
  자동 병합으로 수치가 뒤섞이는 것이 가장 위험하다

**1세션 = 1목적 = 1커밋 = 1브랜치 = 1PR.** 목적이 바뀌면 새 세션을 연다.

---

## 세션 종료 — 반영 절차

순서를 지킨다. 3번을 건너뛰고 파일을 고치지 않는다.

1. 이번 세션에서 **확정된 사실 변경**을 식별한다. 변경이 없으면 아무것도 하지 않는다.
2. **델타 패치 블록을 답변에 출력한다.** 무엇이 왜 바뀌는지, 출처와 신뢰등급을 붙여서.
3. **사용자 승인을 받는다.**
4. 승인된 항목만 파일에서 **해당 줄을 수정한다.**
5. 아래 커밋 메시지 규약에 맞춰 **직접 커밋하고 push한다.** 별도 지시를 기다리지 않는다.

### 브랜치는 즉시 병합한다

세션 = 브랜치 = PR = 병합. 이 4개는 한 묶음이다.

- 커밋·푸시 후 사용자에게 PR 링크와 diff 요약(파일 수, +/− 줄 수)을 알린다
- 병합까지 완주. 병합은 squash, 병합 후 브랜치 삭제

### 승인 대상은 내용이지 git 작업이 아니다

3번 승인은 **레지스트리에 무엇이 들어갈지**에 대한 승인이다.
커밋·푸시는 승인 대상이 아니며 자동으로 처리한다. 승인 없이 파일을 고치지만 않으면 된다.

### 자동 커밋·푸시 안전망

`.claude/settings.json`에 Stop 훅이 걸려 있다. 턴이 끝날 때 미커밋 변경이 남아 있으면
`auto:` 접두사로 자동 커밋하고 현재 브랜치에 push한다.

이 훅이 작동했다는 것은 **내가 규약대로 커밋하지 않았다는 뜻**이다. 정상 경로에서는
훅이 커밋할 것이 없어 조용히 종료한다. `auto:` 커밋이 쌓이면 절차가 어딘가 새고 있는 것이다.

### 전체 재생성 금지

파일 전문을 다시 쓰지 않는다. 변경된 줄만 고친다.
전체를 재작성하면 `git diff`가 전부 변경으로 잡혀 리뷰가 불가능해지고, 이 저장소의 존재 이유가 사라진다.

### 커밋 메시지 규약

```
<스코프>: 한 줄 요약

- 항목: before → after
- 출처: URL 또는 기관명 (시점)
- 등급: A|B|C|D
- 근거: 역산·이식·직접확인 중 무엇인지
```

스코프는 구역명(`장위15`, `상도16`), `정책`, `규칙`, `출처` 중 하나.

예시:
```
장위15: 비교시세 13.0억 → 16.35억 갱신

- 꿈의숲아이파크 84㎡: 13.0억(2025-12, D) → 16.2억(2026-06-20, 국토부)
- 장위자이레디언트 84㎡: 16.5억(2026-05-27, 국토부) 신규
- 출처: 헤럴드경제 2026-07-01 biz.heraldcorp.com/article/10794342
- 등급: B (국토부 실거래 + 매체 1곳)
- Q7 미해결 → 부분해소
```

---

## 하지 말 것

- 승인 없이 파일 수정
- 파일 전문 재생성
- 확인 안 된 수치를 요약표 본표에 채워 넣기 (추정 블록 예외는 `00_PROJECT_BRIEF.md` §9 참조)
- 진행단계가 다른 구역을 하나의 순위표로 줄세우기
- 요청 없이 엑셀 생성. 엑셀은 레지스트리에서 뽑는 산출물이며 저장소에 커밋하지 않는다
- 파일명에 날짜·버전 넣기. 버전은 파일 안 헤더에, 상세 이력은 git log에
- 같은 주제의 파일을 2개 이상 만들기

---

## 추정치 취급

`10_REGION_REGISTRY.md`의 **추정 블록**에 있는 수치는 요약표 본표와 기준선이 다르다.

- 레인지·시나리오로만 읽는다. 점추정으로 취급하지 않는다
- **`인용 금지` 또는 `자리표시자`로 표시된 항목은 계산 근거로 쓰지 않는다.** 문서에 적혀 있다는 사실이 근거가 되지 않는다
- 추정치가 실측으로 승격되면 해당 항목을 추정 블록에서 빼고 본표로 옮긴 뒤, 커밋 메시지에 승격 근거를 남긴다
