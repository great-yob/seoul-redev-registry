# CLAUDE.md

이 저장소는 서울 재개발 구역 투자 비교·검증 프로젝트의 **단일 원본**이다.
이 파일은 git 작업 흐름만 담는다. 판단 규칙·산출식·신뢰등급은 `00_PROJECT_BRIEF.md`에 있다. 중복 서술하지 않는다.

---

## 파일 구조

| 파일 | 역할 | 갱신 빈도 |
| :---- | :---- | :---- |
| `00_PROJECT_BRIEF.md` | 불변 규칙. 역할·산출식·규제전제·신뢰등급·금지사항 | 분기 1회 |
| `10_REGION_REGISTRY.md` | 요약표·추정 포인터·OPEN QUESTIONS. 구역 수는 헤더 참조 | 세션마다 |
| `regions/NN_slug.md` | 구역 상세 + 추정 블록. 구역당 1파일, 헤더 없음(갱신일은 git) | 해당 구역 세션마다 |
| `20_POLICY_CHECKLIST_SOURCES.md` | 정책 동향·체크리스트·출처 | 수시 |
| `30_FIELD_CHECKLIST.md` | **한 구역을 임장할 때 쓰는 공통분** 27항목 + 맨 위에 박는 운영 규칙 5. 현장에서만 확인되는 것만. ID는 페이지 체크 상태의 저장 키라 문구가 바뀌어도 그대로 둔다 | 현장 다녀온 뒤 |
| `DECISIONS.md` | 레지스트리 수치의 판단 근거 로그 | 판단 발생 시 |
| `tools/collect_listings.py` | 매물 자동수집(재개발닷컴) → `regions/*.md`의 `## 매물` 절 통째 교체 + `data/listings/` 스냅샷 기록. `.github/workflows/listings.yml`이 매주 금 04:00 KST 실행 | 자동 · 주 1회 |
| `data/listings/*.jsonl` | 매물 회차 스냅샷. 필터 전 전체를 남기는 기계 축적본(append-only, 회차당 1파일). 형식은 `data/README.md` | 자동 · 주 1회 |
| `tools/vworld_probe.py` | 브이월드 NED API 큐 프로버. 미해결 항목을 태스크로 쌓아 하루 예산만큼 찌르고, 막히면 커서를 남긴다. `--report`로 회차 집계·변화 감지. **md를 고치지 않는다** | 수동 · 세션마다 |
| `data/vworld/queue.json` | 프로버 태스크 큐. 진행 커서·주기·차단 상태를 들고 있어 여러 날에 걸쳐 이어받는다 | 프로버가 갱신 |
| `data/vworld/*.jsonl` | 브이월드 응답 원본 축적(append-only, 회차당 1파일). 판정은 읽는 쪽에서 한다 | 프로버가 추가 |

**충돌 시 우선순위: 10(regions/ 포함) > 00 > 20**

---

## 역할

서울 재개발 디벨로퍼(30년 경력) 관점의 실무 답변. 상세는 `00_PROJECT_BRIEF.md` §2.

핵심만 재확인:
- 서론·과정 생략. 핵심만. 리마인드는 별도 섹션.
- 확정 팩트 / 가능성 / 한계·미검증을 구분해 기대치를 낮춘다.
- 수치는 추측 배제. 실거래 원본과 대조하고, 못 하면 못 했다고 말한다.
- D등급 수치는 계산식에 넣지 않는다.

---

## 세션 시작

1. SessionStart 훅이 `git pull --rebase --autostash`를 자동 실행한다. 실패했으면 수동으로 `git pull`
2. `00_PROJECT_BRIEF.md`·`10_REGION_REGISTRY.md`를 읽고, 스코프 구역의 `regions/` 파일만 읽는다. `20_POLICY_CHECKLIST_SOURCES.md`·`DECISIONS.md`는 정책·출처·근거 스코프이거나 사용자가 물을 때만 읽는다
3. 각 파일 헤더의 갱신일자가 **30일 이상 지났으면 답변 첫머리에 그 사실을 알린다** — 판정 대상은 00·10·20 헤더. `regions/` 파일은 헤더가 없다(갱신일은 git log)
4. 사용자가 스코프를 선언할 때까지 파일을 수정하지 않는다
5. 수치의 근거를 물으면 `10_REGION_REGISTRY.md`와 함께 `DECISIONS.md`도 읽는다

### 여러 기기·세션 병행 시

PC·모바일·클라우드 세션을 동시에 열 수 있다. 같은 파일을 동시에 고치면 충돌한다.

- 작업 전 `git log --oneline -5`로 다른 기기가 올린 커밋을 확인한다
- push가 거부되면(non-fast-forward) 임의로 force push 하지 않는다. `git pull --rebase` 후 재시도하고,
  충돌이 나면 **사용자에게 양쪽 내용을 보여주고 판단을 받는다.** 레지스트리는 단일 원본이므로
  자동 병합으로 수치가 뒤섞이는 것이 가장 위험하다

**1세션 = 1목적 = 1커밋 = 1브랜치 = 1PR.** 목적이 바뀌면 새 세션을 연다.

---

## 세션 종료 — 반영 절차

순서를 지킨다. 3번을 건너뛰고 파일을 고치지 않는다.

1. 이번 세션에서 **확정된 사실 변경**을 식별한다. 변경이 없으면 아무것도 하지 않는다.
2. **델타 패치 블록을 답변에 출력한다.** 무엇이 왜 바뀌는지, 출처와 신뢰등급을 붙여서.
3. **사용자 승인을 받는다.**
4. 승인된 항목만 파일에서 **해당 줄을 수정한다.**
5. 아래 커밋 메시지 규약에 맞춰 **직접 커밋하고 push한다.** 별도 지시를 기다리지 않는다.

### 브랜치는 즉시 병합한다

세션 = 브랜치 = PR = 병합. 이 4개는 한 묶음이다.

- 커밋·푸시 후 사용자에게 PR 링크와 diff 요약(파일 수, +/− 줄 수)을 알린다
- 병합까지 완주. 병합은 squash, 병합 후 브랜치 삭제

### 승인 대상은 내용이지 git 작업이 아니다

3번 승인은 **레지스트리에 무엇이 들어갈지**에 대한 승인이다.
커밋·푸시는 승인 대상이 아니며 자동으로 처리한다. 승인 없이 파일을 고치지만 않으면 된다.

### 자동 커밋·푸시 안전망

`.claude/settings.json`에 Stop 훅이 걸려 있다. 턴이 끝날 때 미커밋 변경이 남아 있으면
`auto:` 접두사로 자동 커밋하고 현재 브랜치에 push한다.

이 훅이 작동했다는 것은 **내가 규약대로 커밋하지 않았다는 뜻**이다. 정상 경로에서는
훅이 커밋할 것이 없어 조용히 종료한다. `auto:` 커밋이 쌓이면 절차가 어딘가 새고 있는 것이다.

`auto-listings:` 접두사는 다르다. `.github/workflows/listings.yml`이 매주 금요일 04:00(KST) 재개발닷컴을 수집해
`## 매물` 절만 교체하고 main에 직접 커밋한 것이다(github-actions[bot]). 정상이며 절차 누수가 아니다.
세션 시작 시 `git pull`로 받아 오면 된다.

### 커밋 보류 마커 (`.claude-hold`)

사용자가 "커밋하지 마"류로 **명시적으로 보류를 요청한 턴**에서는, 저장소 루트에
`touch .claude-hold`로 마커 파일을 만든다. 이 파일이 있으면 Stop 훅은 `git add -A`
전에 조용히 종료하고, 미커밋 변경을 그대로 남겨둔다.

- 사용자가 승인하면 `.claude-hold`를 지우고, 정상 경로대로 직접 커밋·push한다
- 마커는 `.gitignore`에 등록되어 있어 커밋 대상에 포함되지 않는다
- 마커를 지우지 않은 채 세션이 끝나면 다음 세션에서도 보류가 유지된다. 스코프를 이어받으면
  먼저 `.claude-hold` 존재 여부와 사유를 확인한다

### 전체 재생성 금지

파일 전문을 다시 쓰지 않는다. 변경된 줄만 고친다.
전체를 재작성하면 `git diff`가 전부 변경으로 잡혀 리뷰가 불가능해지고, 이 저장소의 존재 이유가 사라진다.

### 커밋 메시지 규약

```
<스코프>: 한 줄 요약

- 항목: before → after
- 출처: URL 또는 기관명 (시점)
- 등급: A|B|C|D
- 근거: 역산·이식·직접확인 중 무엇인지
```

스코프는 구역명(`장위15`, `상도16`), `정책`, `규칙`, `출처` 중 하나.

예시:
```
장위15: 비교시세 13.0억 → 16.35억 갱신

- 꿈의숲아이파크 84㎡: 13.0억(2025-12, D) → 16.2억(2026-06-20, 국토부)
- 장위자이레디언트 84㎡: 16.5억(2026-05-27, 국토부) 신규
- 출처: 헤럴드경제 2026-07-01 biz.heraldcorp.com/article/10794342
- 등급: B (국토부 실거래 + 매체 1곳)
- Q7 미해결 → 부분해소
```

---

## 하지 말 것

- 승인 없이 파일 수정
- 파일 전문 재생성
- 확인 안 된 수치를 요약표 본표에 채워 넣기 (추정 블록 예외는 `00_PROJECT_BRIEF.md` §9 참조)
- 진행단계가 다른 구역을 하나의 순위표로 줄세우기
- 요청 없이 엑셀 생성. 엑셀은 레지스트리에서 뽑는 산출물이며 저장소에 커밋하지 않는다
- 파일명에 날짜·버전 넣기. 버전은 파일 안 헤더에, 상세 이력은 git log에
- 같은 주제의 파일을 2개 이상 만들기. `regions/`는 구역당 1파일이며 중복이 아니다
- **구역을 비교 대상에서 빼는 요청을 삭제로 처리하기.** 요약표 행·`regions/NN_*.md`·`data/`·매물 자동수집은 전부 그대로 두고,
  `site/hidden.json`의 `hidden` 배열에 **요약표 번호만 올린다**. 되살리기는 그 항목을 지우고 재빌드. 사용자 선언 2026-09-15,
  범위·안전장치는 `site/README.md` "구역 숨김" 절. 레지스트리는 단일 원본이라 행을 지우면 근거 사슬과 OPEN QUESTIONS 매칭이 끊긴다
  — 재번호까지 하면 구역 파일 첫 줄 `# N.` 제목과 어긋나 페이지 빌드가 깨진다(2026-09-16 실제 발생, 10 v3.10)
- 데이터 세션에서 `site/`를 열거나 고치기. 페이지는 md에서 자동 생성되는 파생물이다 (`site/README.md`). **단 위의 숨김 한 줄은 예외다** — 데이터 세션이 `site/hidden.json`만은 고친다

---

## 추정치 취급

`regions/` 구역 파일의 **`## 추정` 절**에 있는 수치는 요약표 본표와 기준선이 다르다.

- 레인지·시나리오로만 읽는다. 점추정으로 취급하지 않는다
- **`인용 금지` 또는 `자리표시자`로 표시된 항목은 계산 근거로 쓰지 않는다.** 문서에 적혀 있다는 사실이 근거가 되지 않는다
- 추정치가 실측으로 승격되면 해당 항목을 추정 블록에서 빼고 본표로 옮긴 뒤, 커밋 메시지에 승격 근거를 남긴다
- **`## 매물` 절**(구역 파일)의 호가·매력도 점수는 D등급 참고자료다. 요약표·추정 블록의 어떤 계산에도 쓰지 않는다. 채점 규칙은 `DECISIONS.md` #19, 파서 규약은 `site/README.md` 6번
- **`## 매물` 절에는 예산 필터를 통과한 매물만 실린다** — 호가 6.00억 이상 8.00억 이하(실거주 기준 초기투자금(대출 포함) 상한 8억 · 이상 구간 6~7억, 사용자 2026-09-08 결정, `DECISIONS.md` #19 4·5차). **예외는 하나 — 하한 미만이라도 매력도 점수가 그 구역 예산 통과분 최고점 이상이면 싣는다.** 상한 초과에는 예외가 없다. 범위 밖 매물은 표에 없고 제외 건수만 노트에 남는다. **이 절은 구역 전체 매물 목록이 아니다.** 예산이 바뀌면 `tools/collect_listings.py`의 `PRICE_MIN`·`PRICE_MAX` 한 곳만 고치고 재수집한다
- **`## 매물` 절은 매주 금요일 04:00(KST) GitHub Actions가 자동 교체한다** (`auto-listings:` 커밋). 승인 없이 바뀌는 유일한 예외다. 세션에서 이 절을 손으로 고치지 않는다 — 다음 자동 실행이 덮어쓴다. 구역 사실(권리산정기준일·기준 앵커)이 바뀌면 `tools/collect_listings.py`의 ZONES를 고친다
- **`## 임장` 절**(구역 파일)은 그 구역에서만 찔러야 할 항목이다. 어느 구역에 가든 똑같이 찌르는 공통 27항목은 `30_FIELD_CHECKLIST.md`에 있고 구역 파일에 중복해 적지 않는다. **한 구역 임장 = 공통 27 + 그 구역 특이사항**이며, 페이지도 진행률도 그 합으로 센다. 현장 관찰·중개사 발언은 전부 **C~D등급**이라 요약표·추정 블록의 어떤 계산에도 쓰지 않는다 — 현장에서 등급을 올릴 수 있는 것은 구청·조합이 내주는 **문서 원본**뿐이다
- **임장 페이지의 체크·메모는 브라우저 localStorage에만 있고 git에 없다.** 기기·사람마다 따로이고 캐시를 비우면 사라진다. **저장 단위는 구역**이라 공통 항목도 구역마다 따로 쌓인다 — 답십리489에서 계량기를 센 것이 자양655에서 세어진 것으로 보이면 안 된다. 확정 기록은 페이지의 **내보내기** 마크다운을 세션에 붙여넣어 구역 파일의 `## 임장 기록` 절로 커밋한다 — 델타 패치 승인 절차를 그대로 거친다
- **브이월드 조회는 `tools/vworld_probe.py`로만 돌린다.** 큐(`data/vworld/queue.json`)가 미해결 항목별 진행 커서를 들고 있어 여러 날·여러 세션에 걸쳐 이어받는다. 응답은 `data/vworld/*.jsonl`에 원본대로 쌓이고 **md는 손대지 않는다** — 레지스트리 반영은 델타 패치 승인 절차를 거친다. 판독 규칙(`Referer` 필수 · 판별자는 `manageNo` 공고번호 · `registDt`는 등록일 · 빈 응답≠비대상 · `numOfRows` 상한)은 `00_PROJECT_BRIEF.md` §6.9
- **`## 매물` 절은 판단용 뷰이고, 같은 실행이 `data/listings/YYYY-MM-DD.jsonl`에 필터 전 전체를 회차별로 쌓는다.** 지난 회차 호가·점수는 md가 아니라 이 파일에서 읽는다. 손으로 고치거나 지우지 않는다(append-only). 형식은 `data/README.md`
